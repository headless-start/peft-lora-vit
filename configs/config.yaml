defaults:
  - data: pets
  - model: vit_lora
  - train: default
  - _self_

seed: 42
device: cuda

wandb:
  project: peft-vit-demo
  mode: offline

hydra:
  job:
    chdir: false
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
