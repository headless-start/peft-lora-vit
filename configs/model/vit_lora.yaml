backbone: vit_base_patch16_224
pretrained: true
drop_path_rate: 0.1
lora:
  r: 8
  alpha_factor: 2        # alpha = alpha_factor * r, per the LoRA paper default
  placement: [q, v]      # any subset of q, k, v
  dropout: 0.0
